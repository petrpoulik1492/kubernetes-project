apiVersion: v1
kind: Pod
metadata:
  labels:
  name: httpd-storage
spec:
  containers:
    - image: httpd
      name: httpd 
      volumeMounts:
        - mountPath: /scratch
          name: scratch-volume
  volumes:
    - name: scratch-volume
      emptyDir: 
        sizeLimit: 500Mi
